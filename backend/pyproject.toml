[project]
name = "happy-shot-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "minio>=7.2.15",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.9.6",
]
